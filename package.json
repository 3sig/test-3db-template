{
  "scripts": {
    "start": "bun run install-deps && run-p run-deps run",
    "run": "bun main.js",
    "install-deps": "orchestrator-install",
    "run-deps": "cd orchestrator-deps && ./3suite-orchestrator_v0.0.1_osx-arm"
  },
  "devDependencies": {
    "3lib-orchestrator": "github:3sig/3lib-orchestrator",
    "npm-run-all": "^4.1.5"
  }
}
